name: tsforecast-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.24
  - pandas>=2.0
  - scikit-learn>=1.3
  - joblib>=1.3
  - pytorch>=2.0
  - pyyaml>=6.0
  - matplotlib>=3.7
  - pyarrow>=14.0
  - yfinance>=0.2
  - pytest>=7.0
  - pip
  - pip:
      - transformers>=4.40
      - accelerate>=1.1.0
